import WebSocket from 'ws';
export declare class Stream {
    conn: WebSocket;
    service: string;
    endpoint: string;
    constructor(conn: WebSocket, service: string, endpoint: string);
    send(msg: any): Promise<void>;
    recv(cb: (msg: any) => void): void;
}
